
import {LoginService} from "./login-service"

describe("login service", () => {

	describe("user login routine", () => {
		xit("valid google login returns authoritarian access token", async() => {})
		xit("valid google login write the tokens to token topic", async() => {})
		xit("invalid google login throw an error", async() => {})
	})
})
